[pytest]
; Garante que a raiz do projeto (onde ficam app/, config.py, etc.)
; entre no caminho de importação do Python ao rodar "pytest" direto,
; sem precisar de "python -m pytest" nem de PYTHONPATH manual.
pythonpath = .
testpaths = tests
